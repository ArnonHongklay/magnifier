I"ê(function() {
  ohmpieng.controller('IndexCtrl', function($scope, $state, $stateParams, $http, account, servers) {
    $scope.account = account.data;
    $scope.servers = servers.data.servers;
    if ($scope.servers.length === 0) {
      $state.go("account.setting");
    }
    $scope.currentIndex = 0;
    $scope.chose = function(index) {
      return $scope.currentIndex = index;
    };
    return $scope.parseId = function(val) {
      return parseInt(val);
    };
  });

}).call(this);
:ET